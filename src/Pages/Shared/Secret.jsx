import React from 'react';
import { useLocation } from 'react-router-dom';

const Secret = () => {
    
    return (
        <div>
            Secret Page
        </div>
    );
};

export default Secret;